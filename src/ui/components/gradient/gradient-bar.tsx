import React from "react";
import GradientBarPanel from "./gradiant-bar-panel";

const GradientBar = (props: any) => {
  return <GradientBarPanel {...props}></GradientBarPanel>;
};

export default GradientBar;
